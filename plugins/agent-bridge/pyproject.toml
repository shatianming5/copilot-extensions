[build-system]
requires = ["setuptools>=83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-bridge"
version = "0.4.0-dev411"
description = "Persistent inter-agent communication service for Copilot CLI"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Thomas Michon" }
]
dependencies = [
    "fastapi>=0.141.1",
    # Plain uvicorn (not the [standard] extra) so the HTTP path stays pure
    # Python (h11) on every platform. agent-bridge serves low-traffic localhost
    # SSE -- it uses none of [standard]'s optional speedups (httptools, uvloop,
    # websockets, watchfiles), and httptools in particular ships no win-arm64
    # wheel and would build from source (MSVC) on headless ARM64 hosts. h11 is
    # a core uvicorn dependency and uvicorn.run() falls back to it by default.
    "uvicorn>=0.52.1",
    # Pure-Python WebSocket support for uvicorn (ACP-over-WS transport in
    # routes/acp_ws.py). wsproto is sans-I/O and depends only on h11 (already a
    # uvicorn core dep), so it adds no native build -- unlike the `websockets`
    # speedup in uvicorn[standard]. Keeps the win-arm64 / pure-h11 guarantee.
    "wsproto>=1.2",
    "pyyaml>=6.0",
    "pydantic>=2.0",
    "agent-client-protocol>=0.12.0,<1",
    "agent-ssh-manager",
    "agent-credential-relay",
    "agent-zdd",
    "agent-single-instance-lease",
    "agent-config-migrate",
    "agent-plugin-resolve",
    "agent-procutil",
    "agent-dropin-registry",
    "agent-plugin-activation",
]

[tool.uv.sources]
# Always resolve the vendored lib from the local path, never an index.
# The distribution is named agent-ssh-manager (not the squatted PyPI name
# "ssh-manager") to prevent dependency-confusion; the import module stays
# ``ssh_manager``.
agent-ssh-manager = { path = "libs/ssh-manager" }
# credential-relay framework + host-credential sources (module ``credential_relay``).
agent-credential-relay = { path = "libs/credential-relay" }
# zero-downtime cutover primitives -- the routing table + cutover orchestrator
# (module ``zdd``), extracted from this plugin so other services reuse them.
agent-zdd = { path = "libs/zdd" }
# single-instance lease + supersession self-retire + reconcile-set reaper
# (module ``single_instance_lease``), extracted from this plugin's singleton /
# self_retire so other services inherit one correct "one active per host" impl.
agent-single-instance-lease = { path = "libs/single-instance-lease" }
# config schema versioning + migration (module ``config_migrate``).
agent-config-migrate = { path = "libs/config-migrate" }
# Copilot CLI + Claude plugin resolution -- marketplaces, plugins, repo settings
# (module ``plugin_resolve``). Shared with agent-codespaces.
agent-plugin-resolve = { path = "libs/plugin-resolve" }
# Shared Windows-headless / detached process-spawn kwargs (module
# ``agent_procutil``). Vendored per plugin; byte-identical src across copies.
agent-procutil = { path = "libs/agent-procutil" }
# Resilient tri-state scans/findings for providers.d.
agent-dropin-registry = { path = "libs/dropin-registry" }
# Effective plugin enablement and identity-verified root resolution.
agent-plugin-activation = { path = "libs/plugin-activation" }

[project.scripts]
agent-bridge = "agent_bridge.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 99

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
]

[tool.pytest.ini_options]
markers = [
    "guard: fast structural/contract guard tests run automatically in the pre-push hook",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "S", "B", "A", "RUF"]
ignore = ["S104"]  # binding to 0.0.0.0 is intentional in some configs
