{
  "description": "session/configChanged merges new values into config.values",
  "reducer": "session",
  "initial": {
    "provider": "copilot",
    "title": "Test",
    "status": 1,
    "config": {
      "schema": {
        "type": "object",
        "properties": {
          "target": {
            "type": "string",
            "title": "Target",
            "enum": [
              "workspace",
              "worktree"
            ]
          },
          "baseBranch": {
            "type": "string",
            "title": "Base Branch",
            "enum": [
              "main",
              "develop"
            ]
          }
        }
      },
      "values": {
        "target": "worktree",
        "baseBranch": "main"
      }
    },
    "lifecycle": "ready",
    "activeClients": [],
    "chats": []
  },
  "actions": [
    {
      "type": "session/configChanged",
      "config": {
        "baseBranch": "develop"
      }
    }
  ],
  "expected": {
    "provider": "copilot",
    "title": "Test",
    "status": 1,
    "config": {
      "schema": {
        "type": "object",
        "properties": {
          "target": {
            "type": "string",
            "title": "Target",
            "enum": [
              "workspace",
              "worktree"
            ]
          },
          "baseBranch": {
            "type": "string",
            "title": "Base Branch",
            "enum": [
              "main",
              "develop"
            ]
          }
        }
      },
      "values": {
        "target": "worktree",
        "baseBranch": "develop"
      }
    },
    "lifecycle": "ready",
    "activeClients": [],
    "chats": []
  }
}
